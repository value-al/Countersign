namespace Countersign.Tests;

/// <summary>
/// Placeholder so the test harness has something to run until the public API arrives (#320).
/// Confirms the solution builds, the project reference resolves, and xUnit executes.
/// </summary>
public class ScaffoldTests
{
    [Fact]
    public void Solution_builds_and_test_harness_runs()
    {
        Assert.True(true);
    }
}
