# Normalize line endings: store text as LF in the repo, check out native.
* text=auto eol=lf

# Treat known binary types as binary (never normalize).
*.png binary
*.jpg binary
*.ico binary
*.snk binary
*.nupkg binary
*.snupkg binary
